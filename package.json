{
  "name": "yuno-geospatial-social-media",
  "version": "1.0.0",
  "description": "A modern social media platform that connects people based on their location and shared interests",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd Backend && npm run dev\" \"cd Frontend && npm run dev\"",
    "build": "cd Frontend && npm run build",
    "start": "cd Backend && npm start",
    "install:all": "npm install && cd Backend && npm install && cd Frontend && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
